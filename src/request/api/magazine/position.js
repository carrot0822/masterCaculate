var url = window.glob.url;
var imgurl = window.glob.imgurl;
var fileUrl = window.glob.fileUrl;
export const LocationInfo = {
    LendState:`${url}catamodule/periodicalTbCollectionLocation/editLendState`, //上架修改
    select:`${url}catamodule/periodicalTbCollectionLocation/select`,//分页查询
  };